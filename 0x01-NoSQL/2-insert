// A script that inserts a document in the collection school

if (typeof dbNamse === 'undefined') {
	print("provide database name!");
	quit();
}

// Coneect to database
var db = db.getSiblingDB(dbName);

// Insert document onto school collection
db.school.insert({
	"name": "Holberton school"
});
